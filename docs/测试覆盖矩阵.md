# 测试覆盖矩阵

> 本文档记录仓库当前可复核的测试证据。统计以 2026-09-16 在 macOS / Python 3.12.13 的
> `python -W error::ResourceWarning -m unittest discover -v` 为准；它不是行覆盖率、分支
> 覆盖率或生产并发证明。

## 1. 当前统计

| 指标 | 结果 |
|---|---|
| `test_*.py` 文件 | 26 |
| unittest 发现数 | 165 |
| 通过 | 163 |
| 跳过 | 2（本机未配置隔离 PostgreSQL DSN） |
| P0 MCP registry/调用路径映射 | 12/12 |
| 正式行/分支覆盖率报告 | 未生成 |
| 协调上下文并发 | 双实例 stale writer、双线程超时重派已验证；多进程/高争用压测待补 |

核心测试使用临时真实 SQLite 和真实 `PolicyEngine`，不是 Mock StateStore 或 Mock
PolicyEngine。虚拟时钟控制业务时间；外部设备、审批、维修和库存系统由有状态本地
`LocalDemoAdapter`/`ScenarioEngine` 替身驱动。

## 2. 12 个 P0 MCP 工具的证据边界

权限补充（2026-09-16）：`tests/sql/runtime_security_regression.sql` 在封闭专用 PG 库通过真正非管理员登录，复现并验证修复了审计 UPDATE 过宽授权；同时验证设备/Context 的范围拒绝、客户端设置不能扩权、权限表与供应商拒绝及合法审计追加。它作为 CI 独立 SQL 门禁，不增加上方 Python unittest 发现数，也不证明 MCP 全业务路径均已由受限账号通过。

补充实测（2026-09-16）：首尔无网络 PostgreSQL 16/pgvector 隔离容器已执行原两项条件集成测试，**2 通过、0 跳过**；其中独立只读登录与管理员不同，验证了门店范围、写拒绝及供应商权限。另在真实 postgres_fdw 上验证归档完整行内容及回滚，并实跑合成 A 场景的 pg_dump、独立空库 pg_restore、事件记录对账和离线回放。CI 的 `postgres-archive` 增加禁止 skip 与实际恢复门禁。上述不改写上方无 DSN 的历史全量回归数字，不等于托管 PolarDB、真实平台或语义检索效果验收。

### 2.1 Registry 与 Schema

`test_registry_is_exactly_the_twelve_p0_functions` 固定 5 个查询和 7 个动作的精确 registry：

| 类型 | 工具 |
|---|---|
| 查询 | `query_device_context`、`query_inventory_batches`、`query_sales_holds`、`query_workorder`、`query_approval` |
| 动作 | `apply_sales_hold`、`release_sales_hold`、`apply_batch_disposition`、`create_workorder`、`create_approval`、`decide_approval`、`record_manual_evidence` |

HTTP/runtime schema 测试还验证缺失参数、非对象参数、请求体上限和鉴权边界。这里的
“12/12”表示每个工具都在 registry/契约或场景调用路径中有映射，不表示每个工具都有一个
独立同名测试方法，更不表示 100% 行或分支覆盖。

### 2.2 查询路径

| 查询能力 | 代表性证据 |
|---|---|
| 设备上下文与读数质量 | 六场景评测；`test_scenario_engine_applies_fault_to_same_world`；指挥台场景温度图测试 |
| 库存批次与事件范围 | `test_write_cannot_escape_incident_store_scope`；`test_exposure_assessment_is_batch_specific` |
| 销售冻结回查 | `test_sales_hold_is_idempotent_and_requeryable`；受控释放测试 |
| 工单回查 | `test_scenario_f_partial_workorder_query_blocks_closure` |
| 审批回查与超时 | `test_high_budget_workorder_waits_for_real_approval`；`test_virtual_time_expires_pending_approval` |

### 2.3 动作与安全边界

| 动作能力 | 代表性证据 |
|---|---|
| 冻结、处置、工单 | `test_sales_hold_is_idempotent_and_requeryable`；`test_high_budget_workorder_waits_for_real_approval`；六场景评测 |
| 审批创建/决策 | 金额与处置精确绑定测试；审批超时测试；actor 鉴权测试 |
| 人工证据 | 六场景本地 Adapter/ScenarioEngine 调用路径和闭环评测 |
| 受控解除冻结 | `test_sales_hold_release_requires_release_guard_verification`；`test_sales_hold_release_rejects_a_stale_release_guard`；`test_scenario_b_requires_two_verifications_before_controlled_release` |
| 幂等与重复副作用 | `test_idempotency_key_rejects_a_different_payload`；`test_action_id_cannot_create_a_second_side_effect_with_a_new_key` |

## 3. 六个 P0 Skill 输出契约

`test_all_p0_skill_packages_have_valid_contracts_and_examples` 会校验六个 Skill 的 input/output
Schema 与成功/失败样例。运行时代码还通过 `src/dianxun/skills/contracts.py` 装饰六个主线
入口，并在函数返回时校验 output Schema：

- `detect_coldchain_event`
- `coldchain_risk_assess`
- `diagnose_coldchain_hypotheses`
- `dispatch_stateful_workorder`
- `outcome_verify`
- `review_incident`

测试会删除一个必填输出字段并确认运行时校验明确拒绝，以防静态样例通过但实际返回结构漂移。

`tests/test_skill_registry.py` 另行校验 6 个 stable release 与 manifest/内容 digest 一致、
SemVer 兼容分类、SHA-256 确定性 canary 分桶、retired 禁止新路由，以及旧 Trace DB 迁移后
真实 Skill Span 含 `skill_name/version/digest/channel/registry_version`。AgentTeams 动态证据测试
还会拒绝调用 Trace 中版本或 digest 与 Worker provenance 不一致的证据包。

## 4. 测试文件与实际职责

| 文件 | 当前实际覆盖 |
|---|---|
| `tests/test_stateful_core.py` | SQLite seed/reset、ScenarioEngine、幂等冻结、真实审批、虚拟时间、Incident 聚合门、12 工具 registry、空库启动 |
| `tests/test_adversarial_hardening.py` | 幂等指纹、action 唯一性、事件 scope、审批参数绑定、release guard、新鲜度、HTTP/auth/runtime schema、失败路径 |
| `tests/test_coldchain_workflow.py` | A-F 六场景关键闭环、Top-K Evidence、批次风险、Auditor 独立回查、controlled release、partial 阻断 |
| `tests/test_skill_contracts.py` | 六个 Skill 契约、样例与运行时输出校验 |
| `tests/test_skill_registry.py` | Skill Registry、SemVer、灰度/退役路由、旧库迁移与 Trace release identity |
| `tests/test_evaluation.py` | 场景 Schema、评测可复现性和 M4 gate |
| `tests/test_ablation.py` | 四变体单变量边界、方向性退化与安全门禁 |
| `tests/test_command_center.py` | 六场景只读指挥台、KPI、红线、Auditor verdict、suspect 曲线语义、确定性输出 |
| `tests/test_knowledge_flywheel.py` | 知识候选、脱敏、去重、人工发布边界和 P1 工具 |
| `tests/test_agentteams_artifacts.py` | Worker ZIP、AgentTeams 资源、MCP 静态部署与 Dockerfile |
| `tests/test_agentteams_runtime_evidence.py` | 外部运行证据 bundle 契约与反伪造边界 |
| `tests/test_postgres_contract.py` | Postgres factory 与 SQL profile 静态契约 |
| `tests/test_polardb_integration.py` | SQLite/Postgres 状态一致性与安全 profile；无实例时跳过 |
| `tests/test_supplementary_demo.py` | 缺货和价签补充 Demo 入口 |
| `tests/test_context_lifecycle.py` | tenant 隔离、TTL/WAL、乐观版本、lease/heartbeat、并发唯一重派、checkpoint 重启恢复 |
| `tests/test_incident_concurrency.py` | Incident CAS 真线程竞争、旧数据版本升级、核验记录和聚合原子回滚、嵌套保存点与外层回滚 |
| `tests/test_worker_runtime.py` | 实际本地 HTTP 身份、门店 scope、租约、服务重启、闭环、partial 重试、失败业务写回滚、阶段输出重放、重新遏制处置及 checkpoint 失败回滚 |
| `tests/test_http_transport.py` | 真实慢速 socket、绝对截止、连接容量、超限头部立即拒绝、数据库故障 readiness |
| `tests/test_runtime_recovery.py` | 有界恢复、主控/备用/容量、等待/回执/通知与应急汇合 |
| `tests/test_delivery_portal.py` | 两次完整门户构建哈希、源码不被改写、Node 状态语义、JS 语法、公开字段过滤 |
| `tests/test_project_handbook.py` | 6 项手册回归：1/4/5/13 段完整导出与尾组页码、空讲稿拒绝覆盖、待办状态快照、中文编码链接和同名外部 URL、PDF 实际跳转目标 |

仓库不存在 `tests/conftest.py`、`tests/test_concurrent_state.py`，也没有先前材料中虚构的
多组测试类与方法；不得将规划名称写成已通过证据。

## 5. 分层替身策略

| 层级 | 当前做法 | 证据含义 |
|---|---|---|
| SQLite | 每个核心测试使用隔离的临时真实数据库 | 验证真实 SQL、事务、约束和序列化行为 |
| Policy | 加载真实版本化策略文件 | 验证 actor、action、risk、approval 规则 |
| 业务时间 | 虚拟时钟 | 可重复地推进审批超时和 Evidence 时间戳 |
| 外部系统 | 有状态本地 Adapter/ScenarioEngine | 验证确定性输入、输出和状态变化；不证明真实门店接入 |
| HTTP | 本地标准库 HTTP server | 验证协议、body/auth/tool allowlist 边界 |
| PolarDB | 条件集成测试 | 只有提供外部实例时才形成运行证据 |

## 6. 标准门禁

```bash
uv run --group dev python -W error::ResourceWarning -m unittest discover -v
uv run --group dev ruff check .
uv run --group dev ruff format --check .
uv run python scripts/check_quality_facts.py
uv run python scripts/check_installed_distribution.py --docker-inputs
git diff --check
```

当前尚未配置 `coverage.py`/`pytest-cov` 产物。若比赛要求覆盖率百分比，需要先固定工具版本、
命令、包含/排除规则和 CI 产物，再报告行/分支数字。

## 7. 已覆盖与待补边界

已覆盖：

- 六场景正常与失败闭环；
- 角色、审批、事件 scope、幂等和 release guard；
- HTTP 请求体、鉴权和 runtime 参数 Schema；
- 六个 P0 Skill 的运行时 output Schema；
- 本地 SQLite 和有状态外部系统替身下的确定性回归。
- 协调 Context 的租户隔离、TTL、WAL、乐观锁、Worker lease/heartbeat、超时唯一重派与 checkpoint 恢复。
- 解禁前后以及关闭前安全事实变化、批次缺失/重复、重新止售与错误关闭阻断。
- 仓库外安装 wheel 的真实资源加载、六场景评测及 Demo 入口；Docker 烟测已加入 CI，本机无 Docker 未执行。

待补：

- 目标环境行/分支覆盖率补全与未覆盖路径回归（本地基线见下节）；
- 多进程和高争用 SQLite 压测；
- 真实 PolarDB 事务、RLS、分区、cron、归档、性能与回滚；
- 真实 AgentTeams/QwenPaw 动态运行、延迟、Token、费用与稳定性；
- 真实 IoT、审批、维修、库存系统和门店效果数据。

## 恢复专项

### 全包行与分支基线（2026-09-16）

源码 `87a279f`，macOS/Python 3.12，coverage.py 7.16.1；完整 unittest 161 项中 159 通过、2 项无 PG DSN 跳过，85.802 秒。统计整个 `src/dianxun`：行 5709/7298（78.23%），分支 1470/2304（63.80%），工具合并指标 74.77%。缺失 1589 行、834 分支，部分覆盖分支点 446 个；默认排除 141 行，无新增排除规则。不要将工具合并指标误写成行覆盖率。

复现命令（结果目录不入 Git；生产秘密不放入报告）：

```bash
mkdir -p tmp/coverage-87a279f
uv run --with coverage==7.16.1 coverage run --branch --source=src/dianxun \
  --data-file=tmp/coverage-87a279f/.coverage -m unittest discover -s tests -q
uv run --with coverage==7.16.1 coverage json \
  --data-file=tmp/coverage-87a279f/.coverage -o tmp/coverage-87a279f/coverage.json
uv run --with coverage==7.16.1 coverage html \
  --data-file=tmp/coverage-87a279f/.coverage -d tmp/coverage-87a279f/html
```

这份基线没有合并远端 PG、独立镜像或未插桩子进程，不统计 SQL/前端/部署脚本。低覆盖包括 CLI、旧 CSV 适配器和 PG 驱动路径；报告只表示执行覆盖，不证明断言充分、业务正确或生产安全。T07 的高争用、目标 SLO、托管故障/RPO/RTO 等仍待验。

2026-09-16 补充：首尔隔离 PostgreSQL 上的两项条件测试为 2 通过/0 跳过，其中嵌套执行四条独立受限运行登录的 HTTP 协议回归，4 通过/0 跳过，覆盖重启关闭、身份/租约伪造拒绝、诊断响应丢失恢复及复核失败后重新处置。管理员只准备夹具，请求实际使用非管理员 PG 连接；参与者、批准和实物回执仍为合成数据。四条复用用例不重复计入顶层 161 项；不是实际 AT/Human 或托管 PolarDB 验收。测试环境要求新增 `DIANXUN_TEST_POSTGRES_RUNTIME_DSN`，缺失会失败。

`tests/test_runtime_recovery.py`：22 项本地 HTTP/SQLite 故障回归，覆盖主控与业务 Worker、分层超时、重试预算、备用与容量、并发唯一后继、证据时效、审批/维修等待、迟到回滚、回执对账、应急汇合、旧快照与通知租约。PostgreSQL 并发补入现有条件测试，本机未配置隔离 DSN，保持跳过。

## 实现与评测现状

| 领域 | 当前事实 | 证据与边界 |
|---|---|---|
| 业务核心 | IncidentService、StateStore、Policy、有状态六场景 | SQLite 本地通过，真实业务接口待验 |
| Skill / MCP | 六个 P0 Skill、12 个 P0 MCP、3 个可选知识工具 | [技术契约](技术说明.md)；不是生产覆盖率 |
| 协作与恢复 | Worker HTTP、共享事务/输出恢复、有界监督、Human 运维和 Outbox | Worker / recovery / concurrency 回归；真实平台另验 |
| M4 评测 | 六场景 6/6，Top-1/Top-3 6/6，Evidence 45/45、Trace 26/26 | [报告](../evidence/m4/report.md)，固定 Seed 和虚拟时钟 |
| 安全门 | 五类测试定义的违规为 0 | 不能外推为所有输入或真实食品安全保证 |
| 消融 | no_auditor 的 5/6 场景停于 VERIFY/BLOCKED，0 放行尝试/错误关闭；single_agent 写入被拒；rule_only Top-1 4/6、错派 2 | [消融](../evidence/m4/ablation.md)，不证明多 Agent 普遍优于单 Agent |
| 当前完整回归 | 165 发现 / 163 通过 / 2 条件 skip | 运行日期/环境见本页统计；跳过不算通过 |
| 制品与文档 | Worker ZIP、wheel 安装资源、门户确定性、历史 PDF 原字节保留 | 本地重建不是线上发布或 PDF 更新 |

已完成的修复覆盖商品安全门、Incident CAS/核验原子性、安装资源、Worker 身份/输出/恢复、HTTP 限制与 readiness、门户观测和质量门禁。旧逐项修复过程从 Git 历史追溯；不再维护独立报告。已知业务缺口及外部验收只在[待办](待办.md)维护。

## 二轮反馈新增回归与边界（2026-09-15）

- `tests/test_evidence_guards.py`：5 项，覆盖单样本/长缺测/未来/冲突/重复与时区、错批次/时间/仪器/主体、独立整批处置凭证与同钟旧回执拒绝、场景事件和标记原子提交。
- `tests/test_database_policy_errors.py`：2 项，本地驱动错误注入验证 P0001 分类、诊断脱敏，以及审计拒绝时业务/幂等事务回滚；不是 PostgreSQL 实例运行证据。
- `tests/test_replay.py`：1 项，验证封存 DB 与逐条导出一致、断网只读回放不执行场景、不改变原文件、拒绝覆盖和篡改。
- 既有 Worker 恢复用例增加新轮次独立回执；消融回归要求 rule_only 与 full 共享时间和物理凭证，仅根因排序不同，避免数据时效造成不公平对照。
- 既有 2 项 PolarDB 条件测试扩展真实月首日/维护窗口异常、事务继续可用、分区创建和实际向量写入/检索；本机无实例仍为 skipped。pg_cron、foreign table/OSS 归档、托管 failover 和实店量化均未执行。

完整命令退出码 0，失败 0；真实平台后续按[统一待办 F01–F08](待办.md)和[服务端交接手册](operations/finals-server-handoff.md)验收。本地镜像烟测未执行（无 Docker）；安装 wheel 的隔离烟测不替代容器或云端验收。

部署清单补充检查：使用 kubectl v1.34.1 默认 Kustomize 渲染 `polardb` 和 `polardb-rag`，均退出码 0；核对 Deployment／Service／PVC、运行身份及 P1／RAG 开关。已修复跨目录直接引用 YAML 导致默认加载器拒绝的问题，改用 `agentteams/mcp` base。未连接 Kubernetes 集群，不属于部署运行证明。

继续完善新增 `tests/test_final_operations.py` 的 7 项：场景接入开关/摘要/去重及虚拟时钟隔离、Worker 消息和输出摘要绑定、只读查询及旧上下文兼容、运行库封存/异地文件恢复对账、采集期间 Trace 变化拒绝、pg_dump 参数/诊断边界、HTTP 后台资源与凭证不落盘。pg_dump 单元测试使用子进程替身，真实导出未执行；既有条件测试补入 PG 可重复读/只读/导出快照检查。

本地浏览器实测完成事件列表、设备/商品分别呈现、独立复核与任务历史、退出清空以及其他门店身份无事件；数据为本机合成 HTTP Worker 流程，不是 Element 平台录像。已有 SQLite 运行通过 CLI 封存、离线校验、复制到另一文件后的只读数据对账。wheel 和 Docker 烟测脚本增加后台资源检查，本机仍无 Docker 引擎。

容器验证补充：实现提交 `6dfeb8bf3c43f8f9891380fa8dbf4e2eb47c846d` 的 [GitHub CI container job](https://github.com/XZQ/zhuguang/actions/runs/34984883922/job/104434321511) 已于 2026-09-15 成功完成真实镜像构建、非 root 启动、readiness、鉴权 MCP／Worker runtime 和后台静态资源检查。该环境为 GitHub Runner 的隔离 SQLite／回环烟测，不证明 Kubernetes、真实 AT、PolarDB 或门店接口已运行。

同次 CI 的 Linux 门禁成功；Windows 完整单测以 153 通过／2 跳过结束，耗时 1487.703 秒，后续消融在作业累计 30 分钟时被取消，不能记为该作业全部通过。CI 已将两个系统的单测与制品验证拆成独立作业，保留全部原有检查和每作业 30 分钟上限，避免单测挤占制品检查时间。后续结果按各提交的 CI 记录核验。
