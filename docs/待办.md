# 统一待办

更新日期：2026-09-17。此文件是工程、业务验收、平台与比赛交付的唯一进度入口。各项完成后在这里更新状态，并把长期操作说明放入技术或运维文档，不再新建“待办 01/02/03”文件。

状态按实际交付区分：分析完成、实现完成、本地验证、外部待验。前轮 0131f24 已交付商品证据门禁、原子场景事件、数据库拒绝分类与合成回放；本轮增加场景到运行任务入口、平台消息关联、只读事件后台及现有运行库封存/恢复对账；实际版本以拉取后的 git rev-parse HEAD 为准。没有目标环境证据的事项保留为未完成。

## 2026-09-20 Controller Worker 就绪链路排查：Skill 推送与 Matrix 管理员登录均已修复，readyWorkers 待续验；补齐 B/C/D/F 四个场景 replay 证据包

- **查证方法**：广州 `agentteams-controller`（Up 2 周，镜像 `agentteams-embedded:v1.2.3`）容器内没有独立 CLI 会话；改用容器自身 `AGENTTEAMS_AUTH_TOKEN_FILE`（`/var/run/agentteams/cli-token`）取 Bearer Token，直接查询其内建 REST API `http://100.116.162.22:8090/api/v1/workers` 与 `agt get teams -o json`，取代此前仅凭 Manager 网页或 `docker logs` 的间接判断。`docker logs` 对该镜像为空，真实业务日志只在容器内 `/var/log/agentteams/agentteams-controller-error.log`（用 `docker cp` 只读复制到宿主 `/tmp` 查看，未改动容器）。
- **根因一，已确认并修复：Skill 推送源目录缺失**。`push-worker-skills.sh` 的 `find_skill_source()` 依次在 `~/worker-skills/`、`/root/manager-workspace/worker-skills/`、`/root/agentteams-fs/agents/manager/worker-skills/`（= 宿主 `manager-workspace` 挂载）、`/opt/agentteams/agent/worker-skills/` 四处找 `<skill>/SKILL.md`，四处均不存在店巡四个自定义 Skill（`work-order-dispatch`、`coldchain-risk-assess`、`outcome-verify`、`anomaly-detect`），仅有 AgentTeams 内置的 `git-delegation`、`github-operations`。这是纯部署缺口：仓库里一直有这四个 Skill（`skills/<name>/SKILL.md`），从未被拷贝到广州 Controller 宿主。
  - 修复：`sudo cp -r /home/ubuntu/agentteams-competition/zhuguang/skills/{work-order-dispatch,coldchain-risk-assess,outcome-verify,anomaly-detect} /home/ubuntu/agentteams-competition/manager-workspace/worker-skills/`（源用广州已检出的比赛目录版本 `c5dac7a`），`chown root:root`；容器内 `/root/agentteams-fs/agents/manager/worker-skills/` 确认可见新文件。随后对四个 Worker 分别执行 `docker exec agentteams-controller bash .../push-worker-skills.sh --worker <name> --skill <skill> --no-notify`（Controller 自身 reconcile 循环调用的同一内部路径），五次调用全部 `exit=0`，`mc mirror` 回执确认文件已写入 `agentteams-storage/agents/<worker>/skills/<skill>/`。
  - 验证：修复后约 20 秒，`executor` 的 Worker 状态 `message` 从原先的 `Skill assignment warning: ... Worker skill not found ...` 变为 `None`（Controller 自身 reconcile 循环重新跑过并确认成功，不是本次手工调用直接改写的字段）；约 110 秒后复查，`sentry`/`diagnoser`/`auditor` 仍显示旧的失败 message 未刷新，判断是这三者的 reconcile 尚未在检查窗口内触发，不是修复未生效（源文件已确认挂载可见、手工 mirror 已确认成功）；未继续等待下一次 reconcile 完整验证这三者的 message 清空,该项验证保留待续验。
- **根因二，已确认并修复：Team 因 Matrix 管理员登录 403 无法建房，是此前 `readyWorkers 0/4` 与全部 Worker 卡 `Pending` 的直接原因**。`agt get teams -o json` 显示 `dianxun-patrol-team` 的 `phase":"Failed"`、`leaderReady:false`、`readyWorkers:0`、`totalWorkers:4`，`message` 为：`provision team rooms: team room creation failed: create room "Team: dianxun-patrol-team": admin login: login admin: HTTP 403: {"errcode":"M_FORBIDDEN","error":"M_FORBIDDEN: Wrong username or password."}`。容器 error 日志里同一时段还有一条非致命的同类失败（`reconcileManagerAccess` 尝试把 Manager 移出 Worker 私聊房也是同一 `admin login` 403）。也就是说 Controller 用自己环境变量里的 `AGENTTEAMS_ADMIN_USER`/`AGENTTEAMS_ADMIN_PASSWORD` 登录本地 Matrix（Tuwunel）失败，团队房间建不起来，5 个 Worker CR 因此停在 `Pending`（`diagnoser` 例外显示 `Sleeping`）；这与 Worker 容器本身、心跳、Controller 侧 `[READY]` 上报（每个 Worker 约 20 秒一次，持续正常）完全无关，容器网络层面一切正常。
  - 经用户确认，Controller 环境变量里的 `AGENTTEAMS_ADMIN_PASSWORD` 与 2026-09-17《AgentTeams后台入口与使用说明》里记录、且当时经 Element 网页实测通过的 `admin` 密码不一致（本次会话的安全策略禁止在容器内直接比对/测试密码，改由用户口头确认；密码明文不进本文件，见该说明文档本地副本，不提交 Git）。
  - **修复方式**：不能用 `docker update` 改运行中容器的环境变量，只能重建容器。先 `docker inspect` 导出 `agentteams-controller` 完整 `Env`（45 项）、挂载（`docker.sock`、卷 `agentteams-competition-data:/data`、绑定 `manager-workspace:/root/agentteams-fs/agents/manager`）、网络（`agentteams-net`，别名 `matrix-local.agentteams.io`/`aigw-local.agentteams.io`/`fs-local.agentteams.io`，内部 IP `172.16.2.2`）、端口（`127.0.0.1:28001→8001`、`127.0.0.1:28080→8080`、`127.0.0.1:28088→8088`）与镜像摘要 `sha256:68a0d28a...`；仅将 `AGENTTEAMS_ADMIN_PASSWORD` 一行改为用户确认的当前 Matrix `admin` 真实密码（明文不进本文件），其余 44 项原样保留。`docker stop` 后用 `docker rename` 把旧容器改名保留（`agentteams-controller-preRESTART-20260920033345`，未删除，可回滚），再用相同镜像摘要、相同挂载/端口/重启策略、显式 `--ip 172.16.2.2` 固定内部地址（广州宿主上还有 3 条 `socat` 隧道 [`:8090`、`:9000`、`:6167`] 硬编码转发到这个内部 IP，供团队后台入口/MinIO/Matrix 联邦使用，固定 IP 是为了不弄断它们）重建同名容器。
  - **验证**：容器新起后，`agentteams-controller-error.log` 里不再出现任何 `M_FORBIDDEN`/`admin login` 403；`team room ready ... created:false`（沿用原房间 `!1Trrg2QNy3LDEFqoVk`，未重复建房）；`agt get teams -o json` 的 `phase` 从 `Failed` 变为 `Active`，`message` 变为 `None`。`readyWorkers` 在重建后约 3 分钟内仍在 `0/4` 观察中，需要后续 reconcile 周期确认是否转为 Ready（Worker 是否真正被 Team 接纳，不等于 Team 房间建成）。
  - **新发现的连带问题（未修复，需人工处理）**：同一个 `AGENTTEAMS_ADMIN_PASSWORD` 同时被用作 Controller 登录内嵌 Higress 控制台管理员的凭据；改成上述 Matrix 密码后 Matrix 侧通了，但 Higress 侧反而从"此前 09-15 至今所有历史日志里零次失败"变成持续报错：`restore manager gateway auth: ... higress login with configured credentials failed; fallback admin/admin login also failed: higress login failed for user "admin": HTTP 401 (check higress-console state/secret)`。说明 Matrix 的 `admin` 密码与 Higress 控制台持久化存储（在 `/data` 卷里）的 `admin` 密码早已不是同一个值，此前 Controller 配置的密码只对得上 Higress 那一侧，现在反过来。此改动是本次操作引入的新副作用，不是原有 F02 记录的一部分；Higress 网关登录失败会影响 Manager 的模型网关凭据同步（`ensure consumer manager`），需要人工核实 Higress 控制台真实密码（其持久化状态在 `/data` 卷，不能靠改 env 一次性两边都对上）后再处理，本次未继续深挖或重启 Higress 组件。
- **根因三，已定位到具体代码行、判定为需要改代码才能修的供应商缺陷（未修复）：`readyWorkers` 对外置 Worker 结构性锁死在 0**。修完根因一、二后 `readyWorkers` 仍是 `0/4`；直接读广州 `/home/ubuntu/agentteams-competition/AgentTeams-v1.2.3/agentteams-controller` 源码定位：
  - `team_controller.go:syncTeamMemberStatus` 把 `ms.Ready` 定义为 `member.worker.Status.Phase == "Running"`（必须是持久化在 Worker CR 上的这个精确字符串）。
  - `worker_controller.go` 里外置 Worker（`isEdgeWorker`）的 reconcile 分支只有两条：心跳过期（`edgeHeartbeatStale`）→ 强制置回 `"Pending"`；否则 `Phase == ""` 时初始化为 `"Pending"`；**没有任何分支会把外置 Worker 的 `Phase` 写成 `"Running"`**。
  - 唯一会写 `Phase = "Running"` 的地方是 `lifecycle_handler.go` 的 `POST /api/v1/workers/{name}/ensure-ready`，且只在当前 `Phase` 已经是 `Stopped`/`Sleeping` 时触发；日常的 `POST /api/v1/workers/{name}/ready` 心跳（即日志里持续正常的 `[READY] Worker ... reported ready`）只更新 Controller 进程内存里的一个 map，**从不写回 CR 的 `Status.Phase` 或 `Status.LastHeartbeat`**。
  - 全量搜索整个 Controller 源码树，`Status.LastHeartbeat` 只在 `team_controller.go` 里被读取/转发，**没有任何一处代码给它赋值**——对外置 Worker 恒为空字符串。因此 `edgeHeartbeatStale("", ...)` 恒为 `true`，reconcile 循环每隔约 20–40 秒就会把 Phase 重新压回 `"Pending"`。
  - **现场复现**：对 `diagnoser` 手工调用 `ensure-ready`（当时它已是 `Sleeping`），`phase` 立即变为 `"Running"` 并确认持久化；经用户确认后对 `orchestrator`/`executor`/`sentry`/`auditor` 依次执行 `agt update worker --state Sleeping` + `ensure-ready`，五次调用全部返回 `{"phase":"Running"}`。但约 1 分钟后复查，**五个 Worker 全部又变回 `Pending`**，`readyWorkers` 仍是 `0/4`——与上面的代码分析完全吻合：不是这次操作没做对，是 reconcile 循环按设计会持续把它们压回去。
  - **结论**：在当前这份 `agentteams-embedded:v1.2.3` 二进制里，外置/Edge Docker Worker 拓扑下 `readyWorkers`／`leaderReady` 在设计上就到不了非零——这是镜像本身的缺陷，不是本次任何环境配置、密码或 Skill 问题能修的；`ensure-ready` 只能造成几十秒的假阳性，不能作为稳定状态。要真正修复需要改 Controller 源码（给外置 Worker 补一条 `LastHeartbeat` 写入路径，并让 `computeMemberPhase`/reconcile 分支在心跳新鲜时把 Phase 推进到 `Running`）、重新编译镜像、替换 `agentteams-embedded:v1.2.3` 后重新验证；这已超出决赛提交前可安全操作的范围，本次未尝试改二进制或热补丁。
- **结论**：F02 的 Skill 推送缺口（6 个自定义 Skill 全部补齐并 mirror 成功）与 Matrix 管理员登录阻塞（Team `Failed`→`Active`）均已修复并验证；此前记录的"Worker Token 过期"问题也已在 09-16/09-17 解决并持续正常（本次确认 5 个 Worker 全部纳入 20 分钟续期且成功）。**但 F02 仍不能标记完成，且现在有了比此前更明确的边界**：`readyWorkers`/`leaderReady` 在当前 AgentTeams 镜像下对外置 Worker 拓扑结构性锁死为 0，需要上游/自行改控制器源码才能解决，不是靠本环境配置能修好的；另外本次操作新引入 Higress 控制台管理员登录失败（根因二的连带问题），需要人工核实 Higress 真实密码。对外可以如实说：Worker 容器、鉴权、心跳、Skill 分发、Matrix 团队房间全部就绪且有现场证据；Controller 自身的 Team/Worker 就绪状态聚合逻辑对这种部署形态有已定位到代码行的缺陷，决赛前如需演示真实 AT 闭环，建议走 `docs/operations/finals-server-handoff.md` 第 3 节里 Orchestrator 直接调用 `/runtime` MCP 工具（`runtime_ingest_scenario`/`runtime_poll`/`runtime_assign`/`runtime_complete`）的路径——这条路径不依赖 Team 的 `readyWorkers` 聚合字段，Worker 本身的容器、鉴权、Skill 都已具备条件，本次因时间没有继续验证这条路径是否能跑通。

## 2026-09-20 补齐 B/C/D/F 四个场景的 replay 证据包

- `evidence/replay/` 此前只有 `finals-case-a`（成功，compressor-failure）与 `finals-case-e`（失败，device-recovered-goods-unsafe）；`demo/state/scenarios/` 下还有 4 个一直存在但从未生成证据包的场景：`coldchain-sensor-false-positive.json`、`coldchain-door-left-open.json`、`coldchain-approval-timeout.json`、`coldchain-workorder-query-partial.json`。
- `scripts/capture_replay.py` 原来 `--case` 只硬编码 `success`/`failure` 两个值，各自对应一个场景文件，没有办法从命令行指向其它场景。给它加了 `CASE_SCENARIOS` 映射表，新增 `sensor-false-positive`/`door-left-open`/`approval-timeout`/`workorder-query-partial` 四个 `--case` 取值，分别指向对应场景文件；`success`/`failure` 原有行为不变。
- 依次执行 `uv run python scripts/capture_replay.py --case <name> --output evidence/replay/finals-case-{b,c,d,f}`，四次全部 `passed: true` 退出码 0，返回的 `incident_id` 恰好是 `INC-COLD-B`/`INC-COLD-C`/`INC-COLD-D`/`INC-COLD-F`，与场景文件本身编码的编号对上，确认字母对应关系猜对了。随后用 `uv run --no-sync python scripts/replay.py evidence/replay/finals-case-<x>` 逐个离线复核，四个包全部 `passed: true`。
- **边界**：这四个新包的 `manifest.json.provenance.dirty_worktree` 为 `true`（生成时工作区有未提交改动：本次的 `docs/待办.md` 和 `scripts/capture_replay.py`）。按手册"正式取证应在干净固定版本重新生成"的要求，提交这次改动后应在干净 HEAD 上重新跑一遍这四个 `capture_replay.py` 调用，替换掉当前 dirty 版本再作为正式交付证据。和 A/E 一样，这六个包全部是 `Local synthetic SQLite run`，不代表 AgentTeams、PolarDB、OSS 或真实门店证据。

- **数据库实例就绪**：在 PHX 边缘节点（`129.146.24.141` / `100.95.247.115`）部署并拉起店巡专用 PostgreSQL 14 实例，监听独立端口 `127.0.0.1:5433`，库名 `dianxun`，账号 `ubuntu`，常驻内存仅 27 MB，磁盘仅 310 MB。
- **全量资产灌入对账**：通过高速批处理迁移，全量接管 300 门店、1500 台设备、5100 商品批次（货值 ¥2,719,610.50）、156 起在途真实异常、156 张派工单。
- **43.2 万条高频时序完整注入**：从 `retail_telemetry.parquet` 完整导入 432,000 条热动力学遥测时序（突破 SQLite 抽样限制），联合索引单次历史检索实测仅 0.059 ms。
- **运维指令固化**：提供开箱即用控制脚本 `/home/ubuntu/dianxun-postgres/ctl.sh {status|start|stop|restart|psql}`，连接串为 `postgresql://ubuntu@127.0.0.1:5433/dianxun`。

## 2026-09-17 部署核查与迁移

- 已现场确认上海 Orchestrator/Sentry、首尔 Diagnoser、新加坡 Auditor 容器运行。维护者确认雅加达腾讯下线后，Executor 已迁入 AWS 东京；容器零重启、Agent idle、心跳 ready、Controller 鉴权 HTTP 200、Matrix 同步和店巡 MCP 连接均有当前证据，工作区及 work-order-dispatch Skill 已恢复。
- Executor 已加入现有 20 分钟令牌续期配置，单次续期及后续鉴权读取通过；仍需观察完整有效期窗口。旧“剩余两名 Worker 无法核查”的节点前置问题已部分解除，不能据此推定其业务权限或完整执行链通过。
- F02 继续保持未完成：Controller 对外置 Executor 仍返回 Pending/not_found；尚需修复状态一致性并完成各 Worker 当前认证/权限、真实 Human、双轮委派、受控执行、独立复核及目标 PolarDB 验收。
- Manager、Higress、Element 页面及 Matrix versions 经 SSH 隧道可访问；逐光同站点受保护后台接入仍在准备中，域名发布、管理员/非管理员访问边界和浏览器回归均待验。
- 详细版本、时间与证据边界见[部署与 Worker 核查](operations/deployment-workers-20260917.md)。历史快照保留原时间，不改写为当前健康证明。

## 2026-09-16 执行记录

- 固定版本：本机与广州隔离比赛目录均已对齐 `151e92ec0d6a96fb73335969bfac9832fbef9b40`；广州旧检出保存在 `backup/main-before-f02-20260916-8aa65ba6c02a`。本次没有重启或替换运行中的 Manager、Controller、MCP 或 Worker。
- F01：本机与广州服务器均通过 Ruff、格式和质量事实门禁；完整回归均为 155 项发现、153 通过、2 条 PostgreSQL 条件测试跳过。跳过原因是两处环境都没有隔离 `DIANXUN_TEST_POSTGRES_DSN`、独立只读 DSN 和显式 reset 授权，不能写成 PolarDB 通过。真实接收、销毁及渠道回执仍属外部待验。
- F02：广州 Manager/Controller 已连续运行，但实际查询为 5 个 Worker 全部 `Pending`、团队 READY `0/4`、Human 为 0；运行绑定的 MCP 是 2026-09-03 启动的旧进程，当前 `/live`、`/ready` 均返回 404。服务器没有 PostgreSQL/PolarDB 容器或测试 DSN。因此未执行真实双轮委派、Human 批准、独立复核或 PolarDB 两项条件测试，F02 保持未完成。
- F03：本机及广州均重新生成 A 成功和 E 失败的 SQLite 合成证据包，并通过断网回放校验。广州证据位于受控比赛目录 `evidence/f03-20260916-151e92e`，含 20 条文件 SHA-256；A 为 `CLOSED`，E 为 `CONTAINED`。两包都声明 `Local synthetic SQLite run`，不作为 AT、PolarDB、OSS 或真实门店证据。真实平台消息、PG dump/restore 和同事件 Trace 仍待 F02 环境恢复后执行。
- F08：官网已确认 2026-09-22 在杭州云谷中心进行线下决赛答辩与展示，2026-09-23 为 GOAI DAY；Agent Infra 决赛交付为路演 PPT、现场 Demo、最终代码仓库或等价可验证工程材料。正式答辩时长、报到时段、材料提交窗口和现场技术规范仍以组委会邮件/社群通知为准。因 F02 未完成，本轮不伪造真实平台录屏，也不冻结最终 PPT/PDF/录屏。

赛事信息来源：[Agent Infra 赛道页](https://www.goaihz.com/tracks?track=infra)、[总决赛 70 强公告](https://www.goaihz.com/news/%E6%80%BB%E5%86%B3%E8%B5%9B70%E5%BC%BA%E5%90%8D%E5%8D%95)。网页赛程注明仍可能调整，最终以组委会通知为准。

## 已完成结论

### 2026-09-16 行与分支覆盖率基线

- 在 `87a279f` 上用 coverage.py 7.16.1 对 `src/dianxun` 开启分支统计并执行完整 unittest：161 项发现、159 通过、2 项本机无 PG DSN 跳过，85.802 秒。
- 行覆盖 5709/7298（78.23%），分支覆盖 1470/2304（63.80%），合并覆盖 74.77%；缺失 1589 行、834 分支，部分覆盖分支点 446 个，工具按默认规则排除 141 行。未为抬高数字缩小源码范围或新增排除规则。
- JSON、原始数据与 HTML 保留在本机仓库忽略目录 `tmp/coverage-87a279f/`。命令和边界见[测试矩阵](测试覆盖矩阵.md)；未合并远端 PG/容器或未插桩子进程，不能拿此数字代表生产、安全或全部业务场景覆盖率。

### 2026-09-16 发布镜像与实际 cron 调度补充

- `e2b6b5a` 的实际 MCP 镜像在首尔通过非 root、只读根文件系统、无宿主端口/源码挂载、健康检查、无效凭据拒绝、独立 actor 查询及受限 PG 事件持久化验证。测试容器已清理，Trace 为临时存储；未替换现有 MCP，不等于正式部署或 AT/Human 主链验收。
- 独立 `zhuguang_cron_test` 中实际加载 pg_cron，保留原两个作业命令，临时加速到秒级调度。两租户三门店只入队两条复盘记录，重复运行不新增；自动创建原先不存在的下一月分区 `audit_log_202610`。注入除零失败后，`cron.job_run_details` / `dianxun_cron_health` 记录失败 runid 7、9；恢复原命令后 runid 11 成功。
- 原始记录：首尔 `/home/ubuntu/agentteams-competition/zhuguang-cron-verification-20260916.json`。测试结束已停用两作业并停止专用容器，保留测试库及运行历史；未重启已有 PG/Worker。调度和数据库时区均显式为 `Asia/Shanghai`。
- 边界：这是普通 PostgreSQL 合成加速调度，不是自然 23:00/月度触发、外部告警投递、Agent 复盘执行或托管 PolarDB/OSS 验收。F04 保留这些未完成项。

### 2026-09-16 受限 PostgreSQL HTTP 协议回归

- 首尔封闭 PG 测试环境使用独立非管理员运行登录，四条真实 HTTP 合成回归全部通过：重启后独立关闭、身份/租约伪造拒绝、诊断响应丢失恢复、复核失败后重新遏制执行。管理员只准备夹具，实际 HTTP 请求经受限连接访问 PG；检查登录无 superuser/BYPASSRLS/建库/建角色权限。
- 外层 PG 条件测试 2 通过、0 跳过，内层协议用例 4 通过、0 跳过；顶层发现数量仍为 161，不重复累计内层用例。CI 增加独立运行 DSN，缺失配置不能跳过该验证；配置见[交接手册](operations/finals-server-handoff.md)。
- F02 保持未完成：上述身份、批准与物理回执均为合成夹具，不是实际 AT Worker、Human、门店或托管 PolarDB 验收；未部署新 MCP 或替换现有服务。

### 2026-09-16 独立运行登录与审计不可变修复

- 真实非管理员登录复现：原 `security` 批量 GRANT 包含 `audit_log`，`zhuguang_test_runtime` 能改写已有审计的 actor。修复为 SELECT/INSERT 单独授权，并显式撤销 runtime/HQ 的 UPDATE、DELETE、TRUNCATE、REFERENCES、TRIGGER，升级旧库也生效；迁移标识为 `2026-09-16-security-audit-append-v3`。
- 首尔封闭 PG 新建专用 `zhuguang_runtime_test`。`tests/sql/runtime_security_regression.sql` 使用实际独立连接（不是管理员 SET ROLE），验证合法设备更新、Context 写入、审计追加；客户端伪造租户/HQ/门店不扩大权限；跨门店/租户写入、权限表修改、供应商读取和审计改删清空均拒绝。修复前明确失败，修复后 `RUNTIME_SECURITY_REGRESSION_OK`；非指定数据库在任何写入前拒绝。
- 使用更新后的权限 SQL 重跑两项 PG Python 集成：2 通过、0 跳过；本地完整 161 项发现、159 通过、2 项本机无 DSN 跳过。CI 增加旧 UPDATE 权限升级及真实运行登录门禁；此前提交 `7ad63a9` 的六个 CI job 已全部成功。
- 范围：以上证明指定 SQL 权限，不等于受限 MCP 全业务链或托管 PolarDB 已验收。知识、审计等部分表仍按租户隔离，不能外推为所有表均按门店隔离。
- F02 节点前置仍受阻：Executor 所在 `yjd-tx` 实查离线 3 天；Auditor 节点在线，但既有基础设施 SSH 身份被拒绝，已请求恢复正式访问方式。未猜密码、改 SSH 授权、迁移 Worker 或触碰其它节点上的生产业务。

### 2026-09-16 外置 Worker 凭据续期修复

- 根因：远端 Worker 使用安装时复制的一小时 SA Token；Diagnoser 实测 Token 已于 9 月 3 日过期，自身 Controller 请求为 401，但容器和 QwenPaw API 正常。AgentTeams v1.2.3 的 `containerManaged:false` 路径跳过容器与 Token 投影管理，不能靠重启或把容器标成 managed 解决。
- 实际修复：只为现有 Worker 的同名 ServiceAccount 通过官方 TokenRequest 签发一小时 Token，经 TokenReview 验签/身份/audience 校验后，通过既有 SSH 更新对应私有文件并保留单文件挂载 inode。管理员 Token 不离开 Controller；未关闭认证、延长为长期 Token 或修改 Matrix/模型/MCP 凭据。
- 广州隔离环境新增专用 20 分钟 systemd 续期定时器，首次自动执行退出 0；Diagnoser、Sentry、Orchestrator 均更新成功，三者自身认证请求为 200，Controller 原始日志均有连续自然 READY 接受记录。三容器仍保留 9 月 3 日启动时间，未重启。其它 Worker 尚未纳入本次续期验收。
- 本地新增六项回归；完整 161 项发现、159 通过、2 项本机无 DSN 条件跳过。POSIX 文件投影的四项在 Windows 不执行。隔离 PG 两项零跳过记录继续有效，但不是本轮重新测试。
- F02 仍未完成：Controller 对外置容器仍报告 Pending/not_found；这不是容器停机证据，也不能改成完整协作成功。剩余两名 Worker、当前 MCP、真实 Human、实际任务/Skill/独立复核及托管 PG 仍待验收。操作与停用边界见 [续期说明](../agentteams/ops/README.md)。

### 2026-09-16 PostgreSQL 与归档补充验证

后续实跑更新：`b7ccb05` 的 GitHub CI 六个 job 已全部成功。首尔隔离 PG 的两项 Python 条件测试现已 **2 通过、0 跳过**（管理员执行场景；独立只读登录验证门店范围、写入拒绝和供应商信息边界），包括并发恢复、月界／越窗拒绝、真实 vector SQL 与一致性读快照。此结果补充下方较早的本地 153/2 记录，不把普通 PostgreSQL 写成托管 PolarDB 已通过。

F03 还完成了独立 PG 合成 A 场景的实际 `pg_dump`、另一空库 `pg_restore`（退出码 0）、同事件记录对账及离线回放。报告明确 `synthetic_local_adapter_on_postgresql`、`platform_authenticity_verified=false`，不是 AT Worker 主链、实物或全库灾备验收。首尔受控证据目录为 `/home/ubuntu/agentteams-competition/evidence/f03-pg-20260916`，8 条 SHA-256 全部校验通过。首次 17 客户端向 16 服务端恢复因 `transaction_timeout` 不兼容失败；固定匹配的 16 客户端后两次成功。新增 `scripts/check_postgres_capture.py` 和 `tests/Dockerfile.postgres`，不扩大部署镜像；CI 同时增加两项测试禁止 skip 与真实恢复门禁。

- 首尔新建独立 PostgreSQL 16/pgvector 测试容器，无网络、无宿主端口、限 512 MiB/1 CPU；未修改已有 Worker 或生产服务。镜像固定为 `pgvector/pgvector@sha256:ccc6e83d6e35e931dc7c5def2022729d5a6c370318d099181995567ff1fb4d6b`。
- 首次真实建库发现并修复 `ensure_audit_partition` 未限定目标 schema、错误尝试创建 `pg_catalog` 分区的问题；全新 `zhuguang_bootstrap_test` 的 core/security/archive 应用成功。
- `tests/sql/archive_regression.sql` 在真实 `postgres_fdw` 上先复现等量篡改及更换目标仍通过的漏洞，再验证修复：首次复制、幂等重跑、等量篡改拒绝、目标替换拒绝、缺行拒绝和失败复制回滚均通过。该门禁也已在 `b7ccb05` 的 GitHub CI 成功运行。
- 上述是独立 PostgreSQL 合成测试，不是 PolarDB/OSS、cron、AT 主链或业务价值证明；两项 Python PostgreSQL 条件测试已在后续独立 DSN/只读身份环境执行，见本节首段。Controller 的 Pending 仅是其资源状态，不能单独推断远端容器已停机。
- 本轮最终本地完整回归仍为 155 项发现、153 通过、2 条 PG 条件测试跳过；Ruff、格式、质量事实和 9 条数据库契约／手册专项测试通过。首次受限沙箱运行因禁止本机监听出现 45 项环境错误，允许测试临时监听后完整重跑通过；未将环境错误计为修复前业务失败。

| 原事项 | 已完成部分 | 仍需继续的部分 |
|---|---|---|
| 原待办 01：故障恢复 | 服务端巡查、主控监督、硬截止、有效进展、有限重试、健康备用/容量、等待唤醒、回执对账、Human 运维、通知 Outbox、应急汇合；22 项恢复回归 | 真实平台实例接管、真实外部接口和通知投递验证转入 T03/T05 |
| 原待办 02：真实场景分析 | 产品目标、12 类差距、36 项情境与验收要求已整理 | 风险计算、实物回执、渠道与人员建模仍有代码或外部证据缺口，见 T01/T02/T05 |
| 原待办 03：目标平台验收 | 环境前置、故障注入、版本取证与验收方案已制定；运行手册已完成 | 仓库未提供目标平台验收报告，实际执行与证据归入 T03 |
| 文档整理 | 现行入口收敛，旧方案和交付记录合并；2026-09-15 更新[统一项目与答辩手册](competition/店巡Agent-项目与答辩手册.pdf)，三份复赛 PDF 原位保留 | 此后只维护本文件的状态及对应现行说明；PPT/录屏与正式交付冻结仍属 T04 |

已实现细节见[技术说明](技术说明.md)、[运行与恢复](operations/runtime-recovery.md)；测试结果见[测试矩阵](测试覆盖矩阵.md)。上述“完成”不替代真实商品或平台验收。

## 当前执行清单

按 T01、T02 优先修正可能导致错误建议或虚假完成的缺口；T03 可在具备环境后独立推进。P0 表示风险与可信交付优先，不表示全部 36 项必须在答辩前完成。

| ID | 优先级 | 状态 | 工作与完成标准 |
|---|---|---|---|
| T01 | P0 | 部分实现／本地回归 | 已补时间归一去重、持续超温窗口、单样本/长缺测/未来与过期证据限制、批次和仪器适用性；真实采样周期、运行模式、批次驻留历史与规则签批仍待接入 |
| T02 | P0 | 部分实现／外部待验 | 整批转移/销毁已要求数量、当前动作、独立接收/见证和有效凭证；拆批、范围版本、审批范围变更、POS/SKU/批次及多渠道回查仍待实现接入 |
| T03 | P0 | 外部待验 | 在隔离 AgentTeams、容器和 PostgreSQL 环境固定实际版本，执行下方故障表与身份测试；保存同任务原始记录、脱敏报告和失败复测 |
| T04 | P0 | 部分确认／待冻结 | 官网确认 9 月 22 日杭州云谷中心线下决赛、9 月 23 日 GOAI DAY，交付含路演 PPT、现场 Demo、最终仓库或等价工程材料；正式时长、报到与提交窗口仍待组委会通知。按最新实现修订 PPT/PDF、录制真实平台片段并彩排；代码 SHA、部署、包、讲稿、PDF 和录屏对应，保留提交回执 |
| T05 | P1 | 待实现/接入 | 人员履约：认领、无法执行、当班代理、交班、维修到场、人工接管；Outbox 映射真实值班人与渠道，投递/认领/处置分别可查 |
| T06 | P1 | 待试点 | 真实规则和基线：商品策略签批、设备/探头校准、影子模式、事件标注；比较相同权限与安全门下的人工/规则/Agent 流程 |
| T07 | P1 | 本地覆盖率已测／其余待验 | 全包本地行覆盖 78.23%、分支 63.80%，缺口与统计边界已记录；仍须目标环境覆盖补全、多进程高争用、目标 SLO、RTO/RPO、备份恢复、跨机和数据库/OSS 故障，保留环境及原始样本 |
| T08 | P2 | 后续候选 | 累积暴露、库位容量、召回/已售追溯、无需维修路径、复发治理、跨店知识和新领域适配，按试点需求选择 |

每项验收记录至少含：对应 ID、源码/策略/制品版本、环境与数据性质、输入或故障、预期/实际结果、证据位置、剩余限制。只有通过受影响回归或取得真实环境记录后才能改完成；涉及生产动作和发布另按授权执行。

## 二轮反馈：队友按顺序执行

用户已确认暂时没有可用的真实 AT／Element、PolarDB 与授权数据环境；这些事项留待队友部署服务端后执行，不能标成运行完成。具体配置、命令、证据契约和失败处理见[决赛服务端部署与取证手册](operations/finals-server-handoff.md)。不通过的事项保留失败记录、负责人和复测结果。

| 顺序 | 状态 | 工作与完成标准 | 依赖／交付 |
|---|---|---|---|
| F01 商品证据与基础恢复 | 本机与广州回归通过；外部实物待验 | 2026-09-16 两处均为 153 passed／2 PostgreSQL skipped；测温适用性、缺测未知、整批实物凭证、同钟重做不借旧回执、场景事件与去重标记同事务已回归 | 真实接收/销毁接入仍属 T02；PostgreSQL 跳过不算通过 |
| F02 AT＋PolarDB 主链 | PG 2/0、受限 SQL/四条 HTTP 合成回归与三名 Worker 认证通过；主链待验 | 首尔 PG 已通过场景一致性、并发恢复、分区、vector、只读/RLS 与独立运行登录 SQL/HTTP 路径；仍须验证其余 Worker、当前 MCP 与真实 Human，完成同店两轮真实委派、Skill、批准、执行和独立复核，以及目标 PolarDB 验收 | Controller Pending 不足以证明容器停机；合成 HTTP 回归不等于实际部署和真实 AT/Human/PolarDB 验收 |
| F03 运行证据与后台 | SQLite 双环境与 PG 合成恢复通过；平台待验 | 本机与广州 A/E 合成包回放通过；首尔 PG 合成 A 已实际导出、恢复到独立空库并完成事件对账/离线回放。真实 AT 同事件消息、Worker Trace、失败分支及目标 /operations 仍待验收 | PG 恢复报告只证明合成 A 的事件级一致性，不是全库/RPO/RTO 或平台真实性；平台原始导出仍须核对 |
| F04 分区／归档／cron | PG 分区／foreign table／实际加速 cron 回归通过；自然调度与托管待验 | 首尔实际 PG 已验证归档篡改/缺行拒绝及回滚、分区边界；另经 cron 实际触发验证幂等入队、缺失分区创建、失败记录和恢复成功 | 仍需目标实例自然夜间/月度滚动、外部告警、托管归档与 OSS 证据；确认调度/数据库业务时区，不能把秒级加速或复盘入队当作 Agent 已执行 |
| F05 托管恢复 | 待授权与实测 | 保存已确认写入序号与时间；主备切换后核对丢失/未知结果、重连/恢复、幂等、审批/停售/审计一致性，实测 RPO/RTO | 具体隔离实例、服务商切换权限、故障范围、执行人与复核人；OSS、跨地域灾备和生产 SLO 分别验证 |
| F06 pgvector | 实际 hash 向量写入/查询通过；语义效果待验 | 首尔 PG 已实跑知识审核入库与向量命中；仍须实际语义 embedding、诊断引用、模型/维度/查询/结果/权限证据及授权样本检索效果评估 | hash 测试只证 SQL 链路，不是语义效果；polardb-rag 与 embedding Secret 仍待目标部署；无实测则移除效果主张 |
| F07 业务价值 | 待授权数据与基线 | 同权限/安全门下对比人工、规则、Agent；记录发现到遏制、总耗时、人工操作时间、补证次数及失败率，保留分母和原始样本 | 真实或授权脱敏门店数据与业务负责人；无实测不填改善比例，不用虚拟时钟当实际耗时 |
| F08 演示与材料冻结 | 日期地点已确认；PPT和录屏待冻结 | 官网确认 9 月 22 日杭州云谷中心线下决赛、9 月 23 日 GOAI DAY；主讲聚焦 A 完整处置＋E 证据不足／复核驳回，多 Agent 2～3 页，完整场景和状态机移附录；按验收结果统一代码/镜像/PPT/PDF/录屏 | F02 完成后录真实平台片段；正式时长、报到和提交窗口仍待通知；当前 2026-09-12 PPT/PDF 仍是旧工作稿 |

F01–F08 不替代 T01–T08 的长期业务范围。每项记录固定版本、环境、命令退出码、通过/失败/跳过数、原始证据位置与 SHA-256、执行人与复核人。数据库和未脱敏 Trace 保存在受控证据区，不提交到 Git；同事下载代码后先按手册第 1 节验证，再逐项填结果。

## 业务缺口复核

当前产品目标是维护设备、商品、销售渠道、人员履约四条相互关联的状态链。一次查询成功、数据库处置标记、消息发出或工单完成，分别不能证明现场已安全。

| 编号 | 2026-09-15 当前事实 | 后续工作 |
|---|---|---|
| G01 | 检测已按时间归一去重与 alert_after_minutes 判断持续超温；覆盖不足和可疑读数进入保守遏制 | T01：真实运行模式与采样策略 |
| G02 | 风险评估与独立设备核验已检查未来/过期、重复/冲突记录及采样间隔 | T01：真实校准、数据质量与稳定观察窗口签批 |
| G03 | 单样本、超过 30 分钟缺测和最新数据超过 5 分钟会给出未知覆盖并保持隔离；积分只覆盖已有观测 | T01/T06：入柜/运输/累计暴露历史、真实商品规则 |
| G04 | 人工测温已筛选 incident、batch、device、时间、仪器校准和 product 测点，并有错对象/过期/未来反例 | T01：可信测温采集与仪器台账接入 |
| G05 | transferred/disposed 必须有关联当前动作、整批数量及独立确认的接收/销毁凭证，缺证不能通过 Auditor 或聚合终态 | T02：真实凭证来源、拆批、库存去向与渠道对账 |
| G06 | runtime_open 保存初始设备关联批次；应急入口会检查当前范围一致，但尚无完整补货/拆分/跨店流转范围版本 | T02：范围变化、审批重审和物料去向 |
| G07 | 本地停售/库存/工单与回执有事务和幂等；尚无真实 POS、外卖和库存渠道回查证据 | T02/T03：渠道能力、SKU 映射、外部幂等与对账 |
| G08 | 当前固定场景的设备核验仍依赖工单，关门场景也配置工单 | T08：员工纠偏、无需维修、长期停用的独立路径 |
| G09 | Human 运维、审批与通知 Outbox 协议存在；真实排班、认领、接收和外部投递未形成证据 | T05：真实身份、责任交接和回执 |
| G10 | 自动扫描、硬截止、主控监督、备用和有界等待已实现并有本地回归 | 本地完成；T03 验收真实平台部署，不再重复列为待开发 |
| G11 | Auditor 独立角色回查多个业务表，仍不能证明物理数据来源独立可靠 | T02/T06：接收方、渠道、校准和现场可信来源 |
| G12 | A–F 与自动化门禁为有限合成样本；没有真实误报、漏报、人员负担和成本对照 | T06：影子试点、公平基线和分层指标 |

源码：[巡检](../src/dianxun/skills/anomaly_detect.py)、[风险建议](../src/dianxun/skills/coldchain_risk_assess.py)、[终态判定](../src/dianxun/domain/safety.py)、[Auditor](../src/dianxun/skills/outcome_verify.py)、[runtime](../src/dianxun/runtime.py)、[恢复](../src/dianxun/recovery.py)。

本轮修复了旧分析中的单样本／错批次测温建议和仅凭转移状态认定完成的问题，保留原有审批与独立核验门禁；新旧轮次同一时刻执行时也不能借用旧动作回执。合成回执和字段校验不能替代真实物理接收、可靠身份或食品检测。

## 36 项场景验收

下表保留原 S01–S36，避免在删除旧分析文件时丢失验收范围。“已有合成覆盖”只说明固定场景或本地机制存在；不表示 36 项全部完成。S30/S31 和恢复相关项已按当前代码更新。

### A. 测量与异常发现

| ID | 真实情境／触发条件 | 当前覆盖 | 正确结果与业务约束 | 验收证据 | 优先级 |
|---|---|---|---|---|---|
| S01 | 压缩机故障，温度持续升高 | 已有合成覆盖：compressor-failure | 先按批准策略控制受影响商品，再排查、审批处置及维修；温度恢复不直接放行 | 异常时间线、有效停售回执、工单、批次处置、独立核验 | P0 |
| S02 | 探头故障造成假高温，独立测量正常 | 已有合成覆盖：sensor-false-positive | 保留可疑数据与反证；独立测量必须对象匹配且有效，不按单一高值永久停售 | 原始读数、仪器/对象/时间匹配的复测、告警排除理由、受控解除记录 | P0 |
| S03 | 员工忘关柜门，关门后恢复，不一定需要维修 | 部分覆盖：door-left-open，但当前场景仍要求工单 | 能走员工纠偏与稳定观察；不为满足流程虚建维修单，商品仍单独评估 | 现场纠偏确认、门状态、观察窗口、商品决策及无需维修依据 | P1 |
| S04 | 网关在线但探头停更；读数时间戳在未来 | 已补检测/风险/核验的时间与缺测门禁；批次范围和真实数据质量仍待补 | 旧数据和时钟异常都不能证明当前安全；明确数据故障责任与处置期限 | 观测时间/接收时间、时钟容差、拒绝原因、缺测期间处置记录 | P0 |
| S05 | 重复或乱序读数；两条超温数据实际上来自同一次采样 | 已补本地时间归一、重复去重与冲突反例；设备流待验 | 去重并按事件时间处理；重复值不能增加持续异常或恢复样本数 | 原消息与去重键、排序结果、相同逻辑数据的判定一致性 | P0 |
| S06 | 只有一条正常读数，或两条读数之间缺失很长时间 | 已补本地门禁：单样本与长缺测输出未知 | 覆盖不足输出未知；不把无可积分区间理解为没有暴露，不无条件跨长缺测插值 | 覆盖率、最大间隔、缺口标识；异常期间缺测时不建议直接放行 | P0 |
| S07 | 正常除霜、补货短时开门或清洁导致波动 | 待补；不能用一般开门故障场景代替 | 按批准运行模式识别预期波动；超出该模式允许范围仍需告警 | 设备计划与状态、模式有效期、异常升级条件和误抑制反例 | P1 |
| S08 | 温度过低冻伤不耐冻商品；冷冻商品局部解冻再冻结 | 部分覆盖：有存储上下限字段，主流程侧重超上限 | 不同商品使用不同上下限和暴露策略；温度变低不能自动等同于更安全或更合格 | 商品规则版本、测量类型、上下限与累计暴露反例 | P1 |
| S09 | 柜内不同位置温度不同，两个传感器相互矛盾 | 部分覆盖：有质量标记与复测概念 | 区分空气、模拟商品和实测商品温度；矛盾进入复核，不简单平均掉危险读数 | 探头台账、位置、校准、采样关系及冲突处置 | P0 |
| S10 | 人工测温属于另一批次、另一时间，或仪器校准过期 | 已补本地门禁：按对象、时效、仪器和测点筛选 | 无效或不适用测量不能用于当前批次建议或审计；明确需要补哪项证据 | 正确/错误对象、时效、仪器和测点的正负向记录 | P0 |

### B. 商品、数量与实物处置

| ID | 真实情境／触发条件 | 当前覆盖 | 正确结果与业务约束 | 验收证据 | 优先级 |
|---|---|---|---|---|---|
| S11 | 一柜同时有乳品、鲜食、蔬果，各自规则不同 | 部分覆盖：批次阈值＋统一示例暴露策略 | 每种商品规则有来源、适用范围和批准版本；无法匹配规则则转人工 | 商品标签/企业规则与逐批次建议对应，未知商品拒绝套用默认放行规则 | P0 |
| S12 | 同 SKU 不同批次入柜时间不同，运输或收货阶段已有暴露 | 待补累计暴露和驻留历史 | 按批次在各位置的时间区间计算适用暴露；搬柜和新建事件不重置历史风险 | 收货、入柜、出柜、运输记录与暴露区间；无历史时保留不确定性 | P1 |
| S13 | 事件开启后又补货、移柜或拆分批次 | 部分覆盖：保存初始 affected_batches | 记录范围扩展与分拆关系，扩大范围触发必要遏制与重新审批；不只核验旧快照 | 范围版本、新旧批次关系、范围变化前后审批适用性 | P0 |
| S14 | 标记已转移，但只搬走部分商品或无人接收 | 已补本地门禁：整批接收凭证和当前动作/数量核对；实物接入待验 | 未接收数量保持在途/未知；验证目的库位条件与接收后才能认为该部分完成 | 来源/目标库位、数量守恒、交出与接收主体、目标环境和接收时间 | P0 |
| S15 | 备用冷柜已满或也故障；断电使多柜同时报警 | 待补容量与共同原因建模 | 不给出无法执行的转移方案；按食品风险、时间和容量安排人工选择，合并共同故障信息 | 库位容量与占用、健康状态、风险排序、无法转移升级记录 | P1 |
| S16 | 报损已在系统登记，但商品仍留在货架或被重新拿出 | 已补本地门禁：disposed 状态还需独立销毁凭证；实物接入待验 | 将报损批准、物理隔离、实际销毁/退货分开；未完成前保持限制 | 数量、交接/销毁凭证、操作者、时间、可核验附件及残余库存对账 | P0 |
| S17 | 冷柜没有库存，或库存系统显示零但现场有货 | 部分覆盖：runtime 开事件要求存在批次 | 区分设备维修事件与商品事件；空库存需确认，不能为开事件伪造批次 | 系统库存、现场盘点、设备事件路径及后续补货限制 | P1 |
| S18 | 温度正常，但商品过期、包装破损、污染或正处于召回 | 冷链主流程外；补充演示不等于该联动已实现 | 接受其他合法限制原因；冷链通过不得解除不属于该事件的限制 | 多原因停售记录、解除权归属、外部质检/召回事件关联 | P0 |
| S19 | 发现异常时，部分商品已售出、已交外卖骑手或用于加工 | 待补售后流向与追溯 | 剩余库存控制与已流出商品追踪并行；后续对客/监管行动由责任人授权 | 异常窗口、销售/加工批次映射、可识别范围与未知数量、责任人决定 | P1 |

### C. 跨系统销售控制与一致性

| ID | 真实情境／触发条件 | 当前覆盖 | 正确结果与业务约束 | 验收证据 | 优先级 |
|---|---|---|---|---|---|
| S20 | POS 只识别 SKU，无法精确识别受影响批次 | 外部待验；本地按批次记录 hold | 明确降级控制范围；必要时扩大到 SKU 并现场隔离，记录影响，不能宣称精确批次拦截 | 接口能力清单、批次/SKU 映射、收银拒售与正常批次处理记录 | P0 |
| S21 | 本地 POS 已停售，但外卖、线上或另一收银终端未同步；已支付订单仍在出货 | 外部待验 | 分渠道显示控制状态，处理在途订单；某渠道未知时不能展示“全渠道已阻断” | 各渠道命令与查询回执、订单状态、超时对账和责任人 | P0 |
| S22 | 工单或停售实际成功，但响应丢失 | 本地幂等、回执恢复与事务已覆盖；真实外部接口的响应丢失与对账待验 | 核验原动作，再决定重试；副作用幂等标识跨重试保持稳定 | 原外部单号、回执查询、重试关联、无重复业务动作的对账 | P0 |
| S23 | 同一批次被两个事件同时限制，一件结束，另一件仍未解决 | 部分覆盖：按事件记录 hold，跨事件业务语义待验 | 最终可售由所有有效限制共同决定；关闭一个事件不能清除另一个限制 | 并发事件、原因级 hold、解除前后最终可售状态 | P0 |
| S24 | 门店断网或控制器重连后补传；云端与门店状态冲突 | 外部待验；本地有界恢复已实现，不代表真实门店离线流程 | 离线按现场 SOP 接管，原始时间保留；重连先对账，不能把历史成功当当前有效 | 离线任务与回填记录、事件时间、冲突解决、未受控时间窗口 | P1 |

### D. 人员、审批与维修履约

| ID | 真实情境／触发条件 | 当前覆盖 | 正确结果与业务约束 | 验收证据 | 优先级 |
|---|---|---|---|---|---|
| S25 | 夜间店长不在线，店员忙碌，通知已发但无人认领 | 已有审批等待、Human 运维和 Outbox 协议；真实送达、认领与责任链待接入 | 区分通知送达、阅读、认领和到场；到期升级给当班备份，不无限挂起 | 值班表、接单/未接记录、截止和升级轨迹 | P0 |
| S26 | 交班、请假或委托变更，原责任人离岗 | 待补真实组织和交班 | 交出与接收都有确认；权限随作用域与生效期变化，事件不因换班丢失 | 交班快照、受托权限与有效期、接收确认、遗留动作列表 | P1 |
| S27 | 审批拒绝、逾期，或批准后数量/证据/处置方式发生变化 | 部分覆盖：拒绝/超时及动作关联；范围版本绑定需完善 | 拒绝不被自动重试成批准；重大范围变化重新审批，等待有业务截止 | 决策身份、原审批范围、变化版本、拒绝原因和重新审批记录 | P0 |
| S28 | 维修商拒单、延迟到场、无备件、报价变更或修后复发 | 部分覆盖：创建/完成模拟工单，履约阶段不足 | 有接单、到场、维修、复测状态及超时处置；预算越界重审，复发关联旧事件 | 服务商与技师回执、费用版本、时限、复测和复发关联 | P1 |
| S29 | 员工接管或急需手工操作时，Agent 仍准备执行旧指令 | 部分覆盖：角色限制；接管所有权待补 | 受控接管阻止自动并发写；交回前核验现场与系统状态，旧指令失效 | 接管记录、在途动作核验、旧写拒绝和交回确认 | P0 |

### E. 协作、审计与治理

| ID | 真实情境／触发条件 | 当前覆盖 | 正确结果与业务约束 | 验收证据 | 优先级 |
|---|---|---|---|---|---|
| S30 | 巡检、诊断、执行或审计断联；或持续心跳但无进展 | 本地已覆盖：自动巡查、空心跳/硬截止、有界重试、健康备用和迟到拒绝；平台待验 | 服务端执行硬截止、有限恢复、迟到拒绝和异常升级，不能靠模型自行遵守 | 故障注入、有效进展、预算、唯一后继和旧任务拒绝记录 | P0 |
| S31 | 主控卡住、调度器重启、数据库不可用 | 本地已覆盖：独立主控任务、扫描器恢复/健康、唯一后继；目标环境故障和外部告警待验 | 主控任务独立建模；恢复不重复委派；数据库不可用时限制新写并可观测 | 主控恢复、调度补扫、持久化一致性和外部健康告警 | P0 |
| S32 | 设备恢复，但商品未安全处置或仍有有效限制 | 已有合成覆盖：device-recovered-goods-unsafe | 保持商品限制并指出未满足条件，不因设备恢复关闭事件 | 设备正常记录、商品不通过原因、人工处置责任及禁止放行记录 | P0 |
| S33 | 审计查询只有部分结果、过期结果或无法确认全部对象 | partial 阻断及 runtime 最新 GOOD 读数时效已有本地覆盖；批次适用性和独立来源仍待补 | 缺失不是通过；明确对象与缺口，有限重查后升级 | 响应完整性、证据时间、对象集合、检查点未推进与恢复轨迹 | P0 |
| S34 | 执行者与审计者读取同一错误状态；附件或知识内容诱导越权 | 部分覆盖：角色分离、工具权限；来源可信与模型攻击待验 | 关键结论须有适当独立来源；外部文本只作数据，不能改权限或策略 | 同源错误反例、来源核对、越权请求拒绝、注入评测与脱敏日志 | P0 |
| S35 | 多租户同时出事，回调送错门店；升级策略时旧任务仍在运行 | 部分覆盖：作用域/版本机制；真实环境与策略迁移待验 | 错作用域拒绝；任务固定策略版本，变更影响范围清楚，禁止跨租户恢复 | 并发隔离、回调鉴权、策略版本和回滚/兼容测试 | P0 |
| S36 | 同设备反复告警，员工习惯忽略；复盘只生成长报告而没有改进 | 部分覆盖：知识候选与审核流程，无真实运营基线 | 记录复发、无效告警、重复派工与改进责任；知识变更审核后再评估效果 | 设备日告警基线、复发关联、整改任务、前后对照及策略版本 | P2 |


## 目标平台验收与故障演练

先确认隔离环境、允许的操作范围、数据库、Worker 和 Human 身份，以及可用通知渠道。密钥通过受控配置注入，不粘贴到本文件。固定实际源码 SHA、镜像 digest、Worker 包 hash、部署配置和测试时间。

平台基础验收包括：容器启动与健康/鉴权反例；PostgreSQL 状态一致性、RLS/权限和两个扫描器并发恢复；五角色任务消费、心跳、输出重放、同范围备用接管；真实人工审批和通知投递。外部 POS/维修尚使用替身时明确标记，不能归入真实接口通过。

| 故障 | 要取得的结果和证据 |
|---|---|
| Worker 断联/空心跳，主控卡住/重启 | 硬截止、有界恢复、主控监督和无重复委派 |
| 多扫描器竞争/重启 | 同一前序唯一有效后继，成功阶段不重做 |
| 旧 Worker 迟到提交 | 失效租约拒绝写业务或 checkpoint |
| 工单响应丢失、回执未知 | 原幂等身份和回执核对；未知转人工，不重复建单 |
| 审批/维修等待、拒绝、逾期 | 释放执行资源、原对象唤醒、超时升级，不把 timeout 当批准 |
| 备用/数据库不可用 | 有界等待或升级，外部监测暴露故障、限制危险写 |
| 应急保护与正常流程汇合 | 仅预授权场景开启；遏制不重复，审批及独立核验仍有效 |
| 越权与通知旧代次 | 跨租户/门店、旧通知领取者/代次被拒，回执脱敏 |

记录真实 Project/Room/Task/incident 的委派、Skill name/version/digest、工具请求与身份、租约/检查点/回执和独立核验。使用以下命令校验真实导出，之后仍须人工核对原始日志与录像：

~~~bash
uv run dianxun agentteams-verify <真实证据文件> --output <校验报告>
~~~

校验器通过不代表证据来源真实，禁止复制 fixture 充当平台导出。每项失败修复后重跑受影响故障；外部记录统一关联 T03。

## 材料、现场与试点准入

T04 待确认：陈述/Demo/问答时长，材料命名与提交窗口，场地和报到时段，参会/主讲/操作分工，网络/投屏/音频，差旅政策。旧“9 月 21 日抵杭”只是准备建议，不写成官方要求或已预订。具体讲法与演练命令在[讲稿](competition/决赛讲稿与问答.md)。

| 阶段 | 准入要求 |
|---|---|
| R0 工程与演示 | 本地安全门、版本和制品可复现；取得需要对外声明的真实平台/数据库证据 |
| R1 影子模式 | 仅读真实数据并给建议；设备校准、规则签批、来源可追溯、人工对照和隔离权限合格 |
| R2 受控遏制 | 只开预授权动作；渠道写后回查、真实责任链、人工接管及故障对账通过 |
| R3 有限生产 | 多门店容量、SLO、备份恢复、变更回归；新商品/设备/地区默认转人工 |
| R4 规模复制 | 按设备/商品/地区模板验证，知识审核版本化，建立业务与安全联合治理 |

任何生产阶段都需要：适用商品规则与例外批准、测量台账与 SOP、真实 IAM/排班/审批、POS/WMS/IoT/维修集成和故障报告、安全/审计/密钥轮换、容量与灾备、试点基线，以及模型/Skill/策略变更和人工接管回退演练。

T06 指标保持同口径：无授权/证据不足放行次数及全部请求数；从确认异常到全部所需渠道/实物保护确认的中位数与高分位；未覆盖及未知数量；按设备日误报与独立标注漏报；人工处理分钟与交接次数；不必要停售、报损、误派维修和运行成本；事件证据完整率；预算内恢复与升级队列年龄。安全红线不与效率加权抵消，避免损失的反事实估计不直接计为营收。

现场访谈先补：实际事故流程、商品及 POS 批次识别、探头/时钟/除霜、班次与维修响应、备用库位/接收/销毁、已售与召回、接口幂等与回查、可使用数据及脱敏范围。当前没有实际客户统计或经营收益结论。

## 参考依据与维护

业务范围来自原 2026-09-12 分析，相关资料当时阅读，本次未重新检索网页：[Codex 食品卫生原则](https://doi.org/10.4060/cc6125en)、[食用农产品销售管理办法](https://www.gov.cn/gongbao/2023/issue_10646/202308/content_6898888.html)、[GS1 EPCIS](https://www.gs1.org/standards/epcis)、[FDA Food Code](https://www.fda.gov/food/fda-food-code/food-code-2022)。国际原则与示范规范不替代中国门店商品放行授权；中国所引办法只覆盖其适用对象。

同类工作流参考为 [Danfoss Alsense](https://www.danfoss.com/en/products/dcs/monitoring-and-services/alsense-food-retail/alsense-food-retail-iot-cloud-application-for-supermarkets/)、[Salling 合作公告](https://www.danfoss.com/en-gb/about-danfoss/news/dcs/salling-group-and-danfoss-collaborate-with-microsoft-to-reduce-food-loss-and-energy-usage-in-retail-refrigeration/)、[Testo](https://www.testo.com/en/solutions/saveris-food-store)。前两项是厂商介绍/合作计划；Testo 当时只取得索引摘录，均不作为本项目效果证明。

新增工作优先加入 T01–T08 对应项；明显不同的工作再加 ID。完成项写日期和证据链接，删除重复过程叙述。不要在其他文档复制状态，也不要为了“清空待办”删除尚未解决的问题。
